// IPC handler stubs for cleaner operations
// TODO: Phase 1 implementation

import { ipcMain } from 'electron';

export function registerCleanerHandlers(): void {
  // Handlers are registered in main.ts for now
}
