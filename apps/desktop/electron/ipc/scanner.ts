// IPC handler stubs for scanner operations
// TODO: Phase 1 implementation — wire to Python bridge

import { ipcMain } from 'electron';

export function registerScannerHandlers(): void {
  // Handlers are registered in main.ts for now
  // This file will hold module-specific logic as it grows
}
