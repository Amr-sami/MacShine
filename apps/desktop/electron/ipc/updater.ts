// IPC handler stubs for auto-updater
// TODO: Phase 1 later — uses electron-updater

import { ipcMain } from 'electron';

export function registerUpdaterHandlers(): void {
  // Will handle update check, download, and install
}
